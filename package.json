{
  "name": "expressOsc",
  "version": "0.0.0",
  "description": "Simple Web page for sending OSC to the machine running the server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "",
  "author": "",
  "license": "BSD",
  "dependencies": {
    "node-osc": "~0.2.1",
    "express": "~3.5.0"
  }
}
